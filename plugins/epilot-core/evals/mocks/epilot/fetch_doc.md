---
type: fixed
---

# {{input.path}}

This document describes the requested epilot concept. Configuration is layered: data model (schemas, attributes, relations) → capture (journeys, mappings) → process (automations, workflows) → access (portals, roles). Custom code belongs in an App only when supported configuration cannot deliver the outcome. External systems connect through public APIs, webhooks, events, and Integration Toolkit mappings.
