---
type: llm
---

PASS if the reply frames the solution as an epilot App: it mentions an App manifest or component, the entity surface (tab, widget, or capability on an entity), and the epilot CLI or deployment/installation step, and it presents this as a plan without claiming files were created or changed.
FAIL if the reply suggests this can be done purely with native configuration, omits the App concept entirely, or claims to have created or modified files.
